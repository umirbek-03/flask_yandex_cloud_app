#cloud-config
package_update: true
packages:
  - docker.io
runcmd:
  - systemctl enable --now docker
  - docker run -d --name mini-service -p 8080:8080 -e DATABASE_URL="${DATABASE_URL}" <REGISTRY_OR_IMAGE>:latest
